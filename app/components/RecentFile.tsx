import React from 'react'
import { FileData } from '../types/types'

type props = {
    data:FileData[]
}

const RecentFile = ({data}:props) => {
  return (
    <div>
      
    </div>
  )
}

export default RecentFile
